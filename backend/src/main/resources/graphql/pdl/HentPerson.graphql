query HentPerson($ident: ID!) {
    hentPerson(ident: $ident) {
        adressebeskyttelse {
            gradering
            folkeregistermetadata {
                gyldighetstidspunkt
                opphoerstidspunkt
            }
        }
    }
}